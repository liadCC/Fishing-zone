namespace FishingZone.Core.Input
{
    /// <summary>
    /// The Action Maps defined in FishingZoneControls. Named here so gameplay code switches
    /// context with a typed value instead of a string literal.
    /// </summary>
    public enum InputMap
    {
        Player,
        Boat,
        Fishing,
        UI
    }
}
